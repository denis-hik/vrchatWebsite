export const REQUEST_SERVER = 'https://request.denishik.ru/'

export type worldsTypes = {
    name: string,
    url: string,
    image: string,
    tag: string,
    ex: string,
    gallery: [string]
}