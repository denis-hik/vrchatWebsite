import styled from "styled-components";

export const BottomBlock = styled.div`
  position: absolute;
  bottom: 10px;
  width: 100%;
  left: 0;
  right: 0;
  
  .body-buttons {
    gap: 10px;
    position: relative;
    padding: 10px;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: row;
    padding-bottom: 0;
  }
`